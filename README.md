# jetson-nano-wound-aids
NVIDIA Jetson Nano-based project for wound identification and RAG-based first-aid suggestions.

# initial setup
1. uv sync (to download all packages + .venv)
2. source .venv/bin/activate (to activiate venv)
